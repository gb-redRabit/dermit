import React from 'react';

const Genres = ({text}) => {
    return (
        <div className="text-lg border-solid border-2 border-gray-500 rounded-xl text-white px-5 py-2 text-center">{text}</div>
    );
}

export default Genres;
